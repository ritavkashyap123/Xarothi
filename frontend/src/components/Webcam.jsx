import React from 'react'
import Webcam from "react-webcam";

const WebcamScreen = () => {
  return (
    <Webcam height={window.innerHeight } width={window.innerWidth } />

  )
}

export default WebcamScreen